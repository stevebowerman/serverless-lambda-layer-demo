{
  "name": "layer-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "cd layer && npm run deploy && cd ../lambda && npm run deploy && cd ../",
    "remove": "cd lambda && sls remove && cd ../layer && sls remove && cd ../"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
